# Test binaries and build output
*.test
*.exe
/libcodex

# Profiling and coverage output
*.prof
*.out
*.cov
coverage.txt

# Go workspace files
go.work
go.work.sum

# Editor / OS cruft
.DS_Store

# Performance benchmark module (third-party deps; not part of the library)
/bench/

# Task ledger -- migrated to the central taskman store (~/.taskman/libcodex); the
# repo-local tasks/ dir is legacy and must not re-track (task 125).
/tasks/
