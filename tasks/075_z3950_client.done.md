# 075 -- z3950: pure-Go Z39.50 client -> codex.Record (future version)

New capability. Tier: high effort, structural. Companion to the SRU client [074].
Ref: ANSI/NISO Z39.50 / ISO 23950; bib-1 attribute set; YAZ (reference impl, do
NOT port). Implement from the published standard.

## Motivation

SRU [074] reaches catalogs that speak the modern HTTP protocol, but a large number
of library targets -- legacy OPACs, and some LC/OCLC endpoints -- are **Z39.50
only**. Z39.50 is the same use case and returns the same record schemas as SRU
(MARC21/ISO 2709, MARCXML, SUTRS), so a Z39.50 client extends copy-cataloging reach
to those targets while feeding the exact same decode pipeline. Only the transport
differs: raw TCP + BER/ASN.1 + a stateful session, versus SRU's HTTP + XML.

## Shared shape with SRU (design constraint)

The two clients must converge on one abstraction: **transport -> record bytes ->
decode**. Concretely:

- Present the same `Reader` / `codex.RecordReader` surface as `sru`, so a Z39.50
  search is also a `codex.Convert` source and callers switch transport without
  changing downstream code.
- Reuse (don't duplicate) the record-schema normalization and the per-record MARC
  decode dispatch. Factor the shared bit out of `sru` -- either an unexported
  helper both packages call, or a small `internal/` package if it grows. MARC21
  binary records go through `iso2709.Decode`; MARCXML through `marcxml.Decode`.

## Scope

1. Pure-Go client implemented **from the Z39.50 standard, not a port of YAZ**
   (avoids license entanglement and the ~90% of YAZ -- servers, SRW, ZOOM, GRS-1,
   PQF/CQL -- that is not needed).
2. Session: `Init` (version/options/preferred-message-size negotiation), `Search`
   (Type-1/RPN query, result set), `Present` (fetch records in a chosen syntax),
   `Close`. `Scan` (index browse) later.
3. Query: build a Type-1/RPN query over the **bib-1 attribute set** (use, relation,
   position, structure, truncation, completeness). A small builder mapping common
   access points (title/author/isbn/issn/any) to bib-1 use attributes; do not
   attempt full generality first.
4. Record syntaxes: MARC21/USMARC (BER-wrapped ISO 2709) -> `iso2709.Decode`; also
   MARCXML and SUTRS. Return `*codex.Record` via the shared `Reader`.
5. Present/Reader paging: request records in batches, advancing the result-set
   cursor, ending on the result-set size.

## Hazards

- **BER/ASN.1 is the hard part.** `encoding/asn1` is DER-oriented and does not cover
  the Z39.50 APDUs cleanly (CHOICE types, implicit/explicit context tagging,
  indefinite lengths). Expect to hand-roll a small BER reader/writer for the Z39.50
  PDU set. Scope tightly: land Init/Search/Present with MARC21 first, one syntax.
- Stateful session over raw TCP: connection lifecycle, timeouts (via
  `context.Context`), preferred/maximum message size, and result-set naming.
- Character-set / record-syntax negotiation (Init `otherInfo`, `preferredRecordSyntax`).
- bib-1 attribute mapping is a table with real subtlety (relation/structure combos);
  get the common cases right, document the rest as gaps.
- No live network in tests: capture real BER PDUs (e.g. via a `yaz-client` session
  against a public target) into `testdata/` fixtures and replay them through a
  loopback `net.Pipe`/fake conn; assert Init/Search/Present decode + MARC records.
- **YAZ as a test oracle, not a dependency.** Validate against the reference
  implementation the way `interop_test.go` uses pymarc: shell out to installed YAZ
  tools and `t.Skipf` (with a `brew install yaz` hint) when absent. Two uses:
  (1) spawn `yaz-ztest` on a localhost port and run our client's
  Init/Search/Present against it end-to-end over real TCP+BER; (2) use
  `yaz-client` to capture the canonical PDU byte fixtures above. Do NOT link YAZ
  via cgo -- that would break the pure-Go/no-cgo build; external-process oracle
  tests keep the module dependency-free while still checking interop.
- License: implement from the standard; do not translate YAZ (or any GPL/BSD C)
  source line-by-line.

## Acceptance

- [x] Init + Search + Present returns MARC21 records decoded to `*codex.Record`
      (in-process fake server; no captured fixtures needed -- see Result).
- [x] Exposes the same `Reader` / `codex.RecordReader` surface as `sru`.
- [x] bib-1 query builder for the common access points; RPN Type-1 query encoded.
- [x] Hermetic tests (fake server + BER unit tests + fuzzers); suite green.
- [x] Interop test against a local `yaz-ztest` (skipped when YAZ is not installed),
      mirroring the pymarc `interop_test.go` pattern.
- [x] Deferred pieces enumerated below as a checklist.

## Result

Landed the `z3950` package: a pure-Go, stdlib-only Z39.50 client implemented from
ANSI/NISO Z39.50-1995 (not a YAZ port).

- `ber.go`: minimal BER reader/writer from X.690 -- context/universal classes,
  multi-byte tags (105/110-112/120-121/130/205/211), definite lengths on write,
  definite AND indefinite lengths on read (nested indefinite included),
  `berSize` incremental framing for the TCP stream, INTEGER/BOOLEAN/BIT STRING/
  OID/string values.
- `apdu.go`: Initialize/Search/Present/Close encoders and response decoders;
  Records CHOICE handling (responseRecords, non-surrogate and multiple
  diagnostics); NamePlusRecord -> EXTERNAL unwrapping (octet-aligned and
  single-ASN1-type encodings); DefaultDiagFormat; record-syntax OIDs.
- `query.go`: `Term`/`And`/`Or`/`AndNot` builder over bib-1 use attributes (any/
  title/author/subject/isbn/issn/lccn/id) -> RPNStructure.
- `z3950.go`: `NewClient("host:port/db")`, `Connect` (Initialize negotiation),
  `Conn.Search`/`Present`/`Close`, ctx deadline + cancelation via socket
  deadlines (`context.AfterFunc`). Piggyback disabled (small/medium set bounds
  zero) so retrieval is one Present code path.
- `reader.go`: `Reader` implementing `codex.RecordReader`, lazy dial, Present
  paging, surrogate-diagnostic skip, sticky errors, auto-close at EOF.
- Record decode dispatch by syntax: MARC21 -> `iso2709.Decode` (MARC-8 handled
  there), UNIMARC -> `unimarc.Decode`, MARCXML -> `marcxml.Decode`; SUTRS/OPAC
  raw in `Record.Data`.

### Validated against three independent deployments

- `yaz-ztest` (reference implementation) -- interop test in-suite, skipped
  without YAZ: full session + Reader paging (23/23).
- Koha 'kohadev' Zebra via the localhost:9998 bridge -- 214 hits, all 214 paged,
  identical to the SRU path over the same listener.
- Library of Congress production (`lx2.loc.gov:210/LCDB`) -- 442 hits for
  title=moby dick, exactly matching LC's SRU count (same result set, both
  protocols).

One real interop bug was caught live: PresentRequest field order
(recordComposition must precede preferredRecordSyntax) -- BER SEQUENCEs are
order-sensitive and yaz's ODR closed the session on the swap.

### Notes vs the original scope

- Captured-PDU replay fixtures were unnecessary: the in-process fake server
  (built on the same BER primitives) plus the live yaz-ztest interop test cover
  the decode path better and stay deterministic.
- The sru/z3950 shared-normalization idea didn't materialize: sru normalizes XML
  schema URIs, z3950 maps record-syntax OIDs -- disjoint domains, nothing to
  share. Both expose the same Reader/Record.Decode shape instead.

### Deferred (future tasks)

- [ ] Authentication (idAuthentication open/idPass) -- split to task 077
      (Tier 1, critical for real targets).
- [ ] bib-1 relation/position/structure/truncation attributes -- split to task
      078 (Tier 1, critical for strict servers); proximity stays deferred.
- [ ] OPAC record syntax (holdings) decode -- split to task 079 (Tier 2, ILL).
- [ ] Scan (index browse) and result-set-name management beyond "default" (low).
- [ ] Explain database (low; rarely implemented well in the wild).
- [ ] Character-set negotiation (Init otherInfo); today MARC-8/UTF-8 handling
      rides on iso2709's leader-driven transcoding (low).
- [ ] Server side (a `z3950` listener serving libcodex records) -- out of scope.
